#include <stdio.h>

int main() {
    int width = 10;
    int height = 20;
    int area;

    area = width * height;
    printf("A rectangle with an height of %d cm and width of %d cm. Has an area of %d cm2.", height, width, area);
    return 0;
}
